import styled from 'styled-components';
import tw from 'twin.macro';
import { GreenButton } from './GreenButton';

export const FormButton = styled(GreenButton)`
  ${tw`py-2 px-3 rounded-full text-xs bg-green-700`}
`;
