import styled from 'styled-components';
import tw from 'twin.macro';

export const Wrapper = styled.div`
  ${tw`hidden md:flex max-w-sm w-full flex flex-col gap-12 py-12 px-8 border-l border-gray-200`}
`;
