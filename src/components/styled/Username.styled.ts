import styled from 'styled-components';
import tw from 'twin.macro';

export const Username = styled.span`
  ${tw`text-base sm:text-sm font-medium max-w-[16ch] whitespace-nowrap overflow-x-hidden text-ellipsis`}
`;
